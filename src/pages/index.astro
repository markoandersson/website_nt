---
// src/pages/index.astro
// Basic Layout component assumed (adjust path if needed)
import Layout from '../layouts/Layout.astro';

const projects = [
  {
    title: 'Project Alpha',
    dates: '2021 - 2023',
    company: 'Example Corp',
    tech: ['Java', 'Spring Boot', 'React', 'PostgreSQL'],
    description: 'Led the development of a major web application, focusing on backend architecture and migrating legacy components.'
  },
  {
    title: 'System Modernization Beta',
    dates: '2019 - 2021',
    company: 'Another Inc.',
    tech: ['C#', '.NET Core', 'Azure', 'Angular'],
    description: 'Key developer in a team effort to replace an outdated system with a modern, scalable solution using microservices.'
  },
  {
    title: 'Project Gamma',
    dates: '2017 - 2019',
    company: 'Previous Solutions Ltd.',
    tech: ['Java', 'JavaScript', 'SQL Server'],
    description: 'Developed and maintained features for a large-scale enterprise application, improving performance and stability.'
  }
];

// Extract technologies from projects and order by frequency using functional approach
const technologies = Object.entries(
    // Flatten all tech arrays into a single array and reduce to count occurrences
    projects.flatMap(project => project.tech)
        .reduce((counts, tech) => {
          counts[tech] = (counts[tech] || 0) + 1;
          return counts;
        }, {})
    )
    // Sort by count (descending)
    .sort((a, b) => b[1] - a[1])
    // Extract just the tech names
    .map(([tech]) => tech)
    // Limit to seven most used technologies
    .slice(0, 7);

const keyExpertise = [
  { name: 'Web Application Development', details: 'Java/Spring, C#/.NET Core, JavaScript' },
  { name: 'Legacy Code Replacement & Modernization', details: 'Strategizing and executing migrations' },
  { name: 'Pragmatic Agile Methodologies', details: 'Focus on effective development flow' },
  { name: 'AI in Software Development', details: 'Exploring and utilizing AI tools' }
];

const careerStartYear = 2007;
const yearsExperience = new Date().getFullYear() - careerStartYear;

const pastEmployersCount = [...new Set(projects.map(project => project.company))].length;

const amountOfProjects = projects.length;

// Social media links with icons
const socialMedia = [
  { name: 'GitHub', icon: 'github', url: 'https://github.com/yourusername' },
  { name: 'LinkedIn', icon: 'linkedin', url: 'https://linkedin.com/in/yourusername' },
  { name: 'Twitter', icon: 'twitter', url: 'https://twitter.com/yourusername' },
  { name: 'Email', icon: 'envelope', url: 'mailto:your.email@example.com' }
];
---

<Layout title="Marko Andersson - Portfolio">
  <div class="bg-primary-dark text-primary min-h-screen font-sans">

    <header class="sticky top-0 z-50 bg-primary-medium shadow-md">
      <nav class="container mx-auto px-6 py-3 flex justify-between items-center">
        <div class="text-xl font-bold text-accent">Marko Andersson</div>
        <div class="space-x-6">
          <a href="#home" class="hover:text-accent-light">Home</a>
          <a href="#about" class="hover:text-accent-light">About</a>
          <a href="#projects" class="hover:text-accent-light">Projects</a>
          <a href="#contact" class="hover:text-accent-light">Contact</a>
        </div>
      </nav>
    </header>

    <main class="container mx-auto px-6 py-12">

      <section id="home" class="py-20 md:py-32">
        <div class="w-full h-64 md:h-96 bg-gradient-primary rounded-lg relative overflow-hidden flex items-center">
          <div class="absolute inset-0 w-full h-full flex justify-end">
            <img src="/images/plush_marko_transparent.png" alt="Marko Andersson" class="object-contain h-full w-auto" />
          </div>
          <div class="relative z-10 text-center md:text-left p-8 md:p-12 md:max-w-2xl">
            <h1 class="text-4xl md:text-5xl font-bold mb-2 text-white drop-shadow-lg">Hello.</h1>
            <h2 class="text-3xl md:text-4xl font-bold mb-4 text-white drop-shadow-lg">I'm Marko Andersson</h2>
            <p class="text-xl md:text-2xl text-accent-lighter mb-6 drop-shadow-lg">
              Senior Developer | Legacy Modernization Specialist
            </p>
            <p class="text-lg text-white drop-shadow-md">
              Passionate about building robust software solutions and modernizing existing systems.
            </p>
          </div>
        </div>
      </section>
    </main>

    <section id="technologies" class="w-full py-8 border-t border-b border-primary-light">
      <div class="w-full px-6 flex flex-wrap justify-between items-center text-tertiary text-lg">
        {technologies.map(tech => (
            <span class="whitespace-nowrap">{tech}</span>
        ))}
      </div>
    </section>

    <main class="container mx-auto px-6 py-12">

      <section id="about" class="py-20">
        <h2 class="text-3xl font-bold text-center mb-12 text-accent-light">About Me & Expertise</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-16 items-start">
          <div class="space-y-6">
            {keyExpertise.map(item => (
                <div>
                  {/* Placeholder for potential icon */}
                  {/* <Icon name="code" class="inline-block w-6 h-6 mr-2 text-accent-light" /> */}
                  <h3 class="text-xl font-semibold text-accent-light mb-1">{item.name}</h3>
                  <p class="text-secondary">{item.details}</p>
                </div>
            ))}
          </div>

          <div class="space-y-6">
            <h3 class="text-2xl font-semibold mb-4">My Journey</h3>
            <p class="text-secondary leading-relaxed">
              {/* Placeholder About Me Text */}
              With over {yearsExperience} years in the software industry across {pastEmployersCount} different employers, I've cultivated a deep understanding of the full software development lifecycle. My primary focus lies in [mention something specific, e.g., backend development, full-stack solutions]. I thrive on tackling complex challenges, particularly modernizing legacy systems to improve efficiency and maintainability. I believe in pragmatic agile approaches that streamline development without unnecessary overhead. Exploring how AI can augment the development process is also a key interest of mine.
              <br/><br/>
              Let's connect and discuss how I can contribute to your next project.
            </p>
            <div class="flex justify-around pt-4 border-t border-primary-light">
              <div class="text-center">
                <div class="text-3xl font-bold text-accent-light">{yearsExperience}+</div>
                <div class="text-tertiary">Years Experience</div>
              </div>
              <div class="text-center">
                <div class="text-3xl font-bold text-accent-light">{pastEmployersCount}</div>
                <div class="text-tertiary">Past Employers</div>
              </div>
               <div class="text-center">
                  <div class="text-3xl font-bold text-accent-light">{amountOfProjects}+</div>
                  <div class="text-tertiary">Projects Delivered</div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section id="projects" class="py-20 border-t border-primary-light">
        <h2 class="text-3xl font-bold text-center mb-12 text-accent-light">Featured Projects</h2>
        <div class="space-y-12">
          {projects.map(project => (
              <div class="bg-primary-medium p-6 rounded-lg shadow-lg">
                <h3 class="text-2xl font-semibold mb-2 text-accent-light">{project.title}</h3>
                <div class="flex flex-wrap gap-x-4 gap-y-2 text-sm text-secondary mb-3">
                  <span>{project.company}</span>
                  <span>|</span>
                  <span>{project.dates}</span>
                </div>
                <div class="flex flex-wrap gap-2 mb-4">
                  {project.tech.map(t => (
                      <span class="bg-primary-light text-light text-xs font-semibold px-2.5 py-0.5 rounded">{t}</span>
                  ))}
                </div>
                <p class="text-light leading-relaxed">{project.description}</p>
              </div>
          ))}
        </div>
      </section>

      <section id="contact" class="py-20 text-center border-t border-primary-light">
        <h2 class="text-3xl font-bold text-center mb-6 text-accent-light">Get In Touch</h2>
        <p class="text-lg text-secondary mb-8">Interested in collaborating? Send me a message!</p>
        <div class="flex justify-center space-x-8 mt-6">
          {socialMedia.map(social => (
            <a href={social.url} target="_blank" rel="noopener noreferrer" class="text-secondary hover:text-accent-light transition duration-300">
              <span class="sr-only">{social.name}</span>
              <i class={`${social.icon === 'envelope' ? 'fa-solid' : 'fa-brands'} fa-${social.icon} text-3xl`} aria-hidden="true"></i>
            </a>
          ))}
        </div>
      </section>

    </main>

    <footer class="bg-primary-medium py-6 mt-12">
      <div class="container mx-auto px-6 text-center">
        <div class="text-tertiary text-sm">
          &copy; {new Date().getFullYear()} Marko Andersson. All rights reserved.
        </div>
      </div>
    </footer>

  </div>
</Layout>
